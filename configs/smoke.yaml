seed: 17
seeds: [17]
device: cpu
data_mode: toy
data_path: data/raw/icews14
output_root: results
target_coverage: 0.9
deletion_rates: [0.0, 0.2]
train_fraction: 0.6
calibration_fraction: 0.2
calibration_role_fractions: [0.25, 0.30, 0.15, 0.30]
rolling_window: 100
half_lives: [2.0, .inf]
adaptive_selector_ridge: 1.0
adaptive_coverage_tolerance: 0.02
max_set_sizes: [1, 3, 5, 10]
embedding_dim: 8
epochs: 2
batch_size: 16
negatives: 2
training_margin: 1.0
learning_rate: 0.05
weight_decay: 0.0
eval_every: 1
patience: 1
min_score_std: 0.0
